import 'package:flutter/material.dart';

class CampeonatosScreen extends StatelessWidget {
  const CampeonatosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Campeonatos Screen'),
      ),
    );
  }
}
